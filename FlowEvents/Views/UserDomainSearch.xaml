<Window x:Class="FlowEvents.Views.UserDomainSearch"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlowEvents.Views"
        mc:Ignorable="d"
        Title="UserDomainSearch" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Параметры поиска -->
        <GroupBox Header="Параметры поиска" Grid.Row="0" Margin="0,0,0,10">
            <StackPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Text="Поиск:" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBox Grid.Column="1" Text="{Binding SearchOptions.SearchTerm}" 
                         ToolTip="Имя, фамилия, логин или email"/>

                    <TextBlock Grid.Column="2" Text="Макс. результатов:" 
                           VerticalAlignment="Center" Margin="10,0,5,0"/>
                    <ComboBox Grid.Column="3" SelectedItem="{Binding SearchOptions.MaxResults}">
                        <ComboBoxItem Content="10"/>
                        <ComboBoxItem Content="50"/>
                        <ComboBoxItem Content="100"/>
                        <ComboBoxItem Content="500"/>
                    </ComboBox>
                </Grid>

                <CheckBox Content="Только активные пользователи" 
                      IsChecked="{Binding SearchOptions.OnlyActive}"/>
            </StackPanel>
        </GroupBox>

        <!-- Управление -->
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Button Content="🔍 Поиск" Command="{Binding SearchCommand}" 
                Style="{StaticResource PrimaryButton}"/>
            <Button Content="📋 Копировать выделенное" Command="{Binding CopySelectedCommand}"/>
            <Button Content="👤 Выбрать пользователя" Command="{Binding SelectUserCommand}"/>
        </StackPanel>

        <!-- Результаты -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Users}">
            <!-- Колонки как в предыдущем примере -->
        </DataGrid>
    </Grid>
</Window>
