<Window x:Class="FlowEvents.Views.UserInfoWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FlowEvents.Views"
        mc:Ignorable="d"
        Title="Информация о пользователе Windows" 
        Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#f0f0f0">
    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="3"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#cccccc"/>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="Информация о текущем пользователе Windows" 
                   FontSize="18" FontWeight="Bold" HorizontalAlignment="Center" 
                   Margin="0,0,0,20" Foreground="#2c3e50"/>

        <!-- Основная информация -->
        <Border Grid.Row="1" Background="White" BorderBrush="#bdc3c7" 
                BorderThickness="1" CornerRadius="5" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Логин -->
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Логин:" FontWeight="Bold"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Login}" Name="txtLogin"/>

                <!-- Отображаемое имя -->
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Отображаемое имя:" FontWeight="Bold"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding DisplayName}" Name="txtDisplayName"/>

                <!-- Полный логин -->
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Полный логин:" FontWeight="Bold"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding FullLogin}" Name="txtFullLogin"/>

                <!-- Домен -->
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Домен/Компьютер:" FontWeight="Bold"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Domain}" Name="txtDomain"/>

                <!-- Email -->
                <TextBlock Grid.Row="4" Grid.Column="0" Text="Email:" FontWeight="Bold"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Email}" Name="txtEmail"/>

                <!-- Тип пользователя -->
                <TextBlock Grid.Row="5" Grid.Column="0" Text="Тип пользователя:" FontWeight="Bold"/>
                <TextBox Grid.Row="5" Grid.Column="1" Text="{Binding UserType, Mode=OneWay}" Name="txtUserType"/>

                <!-- SID -->
                <TextBlock Grid.Row="6" Grid.Column="0" Text="SID:" FontWeight="Bold"/>
                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding SID}" Name="txtSID"/>

                <!-- Описание -->
                <TextBlock Grid.Row="7" Grid.Column="0" Text="Описание:" FontWeight="Bold"/>
                <TextBox Grid.Row="7" Grid.Column="1" Text="{Binding Description}" Name="txtDescription"/>

                <!-- Distinguished Name -->
                <TextBlock Grid.Row="8" Grid.Column="0" Text="Distinguished Name:" FontWeight="Bold"/>
                <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding DistinguishedName}" 
                         Height="40" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"/>
            </Grid>
        </Border>

        <!-- Кнопки -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Обновить" Click="RefreshButton_Click" Width="80" Margin="5,0"/>
            <Button Content="Копировать все" Click="CopyAllButton_Click" Width="100" Margin="5,0"/>
            <Button Content="Закрыть" Click="CloseButton_Click" Width="80" Margin="5,0"/>
        </StackPanel>
    </Grid>
</Window>
